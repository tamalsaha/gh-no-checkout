name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
#     - uses: actions/checkout@v2

    - name: Run a multi-line script
      run: |
        ls -l
        echo "________________"
        pwd
        env | sort
        cd ..
        

    - name: Run a multi-line script
      run: |
        pwd
        
